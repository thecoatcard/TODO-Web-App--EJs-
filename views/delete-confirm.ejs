<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirm Deletion</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
</head>

<body class="bg-gray-100 text-gray-800 flex items-center justify-center min-h-screen px-4">
    <div class="w-full max-w-lg mx-auto p-8 bg-white rounded-2xl shadow-lg border border-gray-200">
        <div class="text-center">
            <h1 class="text-3xl font-bold mb-4" style="color: #F87171;">Delete Task</h1>
            <p class="text-gray-600 text-lg mb-2">Are you sure you want to delete this task?</p>
            <p class="text-gray-800 font-semibold text-xl mb-8 bg-gray-100 p-3 rounded-lg">"<%= task.title %>"</p>
        </div>

        <div class="flex items-center justify-center gap-4">
            <a href="/" class="font-bold text-gray-700 py-3 px-6 rounded-lg bg-gray-200 hover:bg-gray-300 transition-all">
                Cancel
            </a>
            
            <form action="/delete/<%= task._id %>?_method=DELETE" method="post">
                <button type="submit" 
                        style="background-color: #F87171;"
                        class="font-bold text-white py-3 px-6 rounded-lg hover:opacity-90 transition-all">
                    Delete
                </button>
            </form>
        </div>
    </div>
</body>
</html>