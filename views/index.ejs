<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Dashboard | Calming Pastel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css"> <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
     <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    
</head>

<body class="bg-gray-100 text-gray-800">

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

        <%- include('./partials/header') %>

        <%- include('./partials/flash-messages') %>

        <div class="fade-in md:flex md:gap-8" style="animation-delay: 100ms;">
            <div class="md:w-3/5 lg:w-2/3 space-y-8 mb-8 md:mb-0">
                <div class="p-6 ui-card">
                    <h2 class="text-2xl font-bold text-gray-900 mb-4">Search Tasks</h2>
                    <form action="/" method="get" class="relative">
                        <input type="text" name="search" placeholder="Search by keyword..." value="<%= search %>"
                            class="themed-input w-full pl-4 pr-10 py-3 rounded-lg" />
                        <button type="submit"
                            class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-purple-500 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                            </svg>
                        </button>
                    </form>
                </div>

                <div class="p-6 ui-card">
                    <h1 class="text-2xl font-bold text-gray-900 mb-4">Add New Task</h1>
                    <form action="/submit" method="post" class="flex flex-col gap-4">
                        <input type="text" name="title" placeholder="Title of the task" required
                            class="themed-input px-4 py-3 rounded-lg" />
                        <textarea name="description" placeholder="Details of the task" rows="4" required
                            class="themed-input px-4 py-3 rounded-lg"></textarea>
                        <div>
                            <label for="deadline" class="block mb-2 text-sm font-medium text-gray-600">Deadline</label>
                            <input type="text" name="deadline" id="deadline" placeholder="Select a date..." required
                                class="themed-input w-full px-4 py-3 rounded-lg" />
                        </div>
                        <button type="submit"
                            style="background-color: #A78BFA;"
                            class="font-bold text-white py-3 rounded-lg hover:opacity-90 transition-all transform hover:scale-[1.02] focus:outline-none focus:ring-4 focus:ring-purple-500/50">
                            Add Task
                        </button>
                    </form>
                </div>
            </div>

            <div class="md:w-2/5 lg:w-1/3">
                <%- include('./partials/task-list', { tasks }) %>
            </div>
        </div>
    </div>

    <script>
        // Initialize Flatpickr Date Chooser Calendar
        flatpickr("#deadline", {
            altInput: true,
            altFormat: "F j, Y",
            dateFormat: "Y-m-d",
            minDate: "today" // Prevents selecting past dates
        });
    </script>

</body>
</html>